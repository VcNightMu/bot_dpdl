# 单元测试任务 - 宝生玛格

## 日期: 2026-06-01
## 负责人: 宝生玛格
## 指派人: 艾玛（开发SE）

## 任务要求

对你负责的模块编写**函数级单元测试**，测试文件统一存放在 `F:\ArkCodes\bot_dpdl\tests\` 目录下。

## 你负责的模块

1. `formatter/category.py` - #流派 格式化
2. `formatter/help.py` - #help 格式化
3. `formatter/record.py` - #查 记录格式化
4. `formatter/stale.py` - #待压人 格式化
5. `formatter/uncleared.py` - #未通关 格式化
6. `formatter/video.py` - 视频链接处理
7. `resolver/category.py` - 流派名归一化
8. `resolver/episode.py` - 活动名精确匹配
9. `resolver/operation.py` - 关卡名精确匹配

## 测试文件命名规范

- 测试文件放在 `F:\ArkCodes\bot_dpdl\tests\` 目录下
- 命名格式：`test_<模块名>.py`
- 例如：`test_category_fmt.py`, `test_record.py`, `test_resolver_operation.py` 等
- 或者按模块组命名：`test_formatter.py`（包含所有formatter测试）, `test_resolver.py`（包含所有resolver测试）
- `__init__.py` 不需要创建（pytest 自动发现）

## 测试要求

1. **函数级覆盖**：每个公开函数/方法至少一个测试用例
2. **正常路径 + 边界情况**：至少覆盖正常输入和至少一种异常/边界输入
3. **Mock 外部依赖**：HTTP请求、数据库等必须 mock（你这边主要是纯函数，不太需要mock）
4. **使用 pytest 框架**：import pytest, 用 pytest 风格（不需要继承 unittest.TestCase）
5. **独立可运行**：每个测试文件可以单独 `pytest test_xxx.py` 运行

## 测试目录结构

```
tests/
├── conftest.py            # 可选：共享fixtures
├── test_formatter_category.py   # 你写
├── test_formatter_help.py       # 你写
├── test_formatter_record.py     # 你写
├── test_formatter_stale.py      # 你写
├── test_formatter_uncleared.py  # 你写
├── test_formatter_video.py      # 你写
├── test_resolver_category.py    # 你写
├── test_resolver_episode.py     # 你写
├── test_resolver_operation.py   # 你写
```

## 注意事项

- 先读一下你负责的源码文件，了解函数签名和逻辑
- 测试中 import 路径需要 `sys.path.insert(0, r"F:\ArkCodes\bot_dpdl")`
- resolver 模块的测试重点是：别名映射是否正确、边界字符串（空串、特殊字符）
- formatter 模块的测试重点是：输出格式是否符合QQ消息规范、空数据处理
- video.py 需要 mock httpx 请求（B站API）
- 写完后确保 `pytest tests/test_formatter_category.py` 等能通过
